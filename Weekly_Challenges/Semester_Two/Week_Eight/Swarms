// Swarms.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include "Agent.hpp"
#include "Functions.hpp"
#include "Swarm.hpp"

int main()
{
    std::cout << "Hello World!\n";

    /*
    int i;
    int size = 10;
    double* xOne = x(size);
    double* fNought = f0(size);
    double* fOne = f1(size);
    double* fTwo = f2(size);
    */
    
    int i;
    int size = 200;

    double randy = randDoubOne();
    cout << randy;

    //swarm SwarmOne = swarm(10, 10, 20);
    agent agentTest = agent(1, size, 4, 2);
    double* posTest = agentTest.positionArr(0.3);
    
    for (i = 0; i < size; i++) {
        cout << "\n" << posTest[i];
    }
    /*
    for (i = 0; i < size; i++) {
        ///Everything comes out as 6.whatever to the -6. Don't know why
        cout << "\n" << xOne[i] << "\t" << fNought[i] << "\t" << fOne[i] << "\t" << fTwo[i];
    }
    */
}


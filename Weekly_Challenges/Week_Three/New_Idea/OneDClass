#include <iostream>
#ifndef ONEDMATHEADERDEF
#define ONEDMATHEADERDEF

class oneDMat {
public:
	int rows;
	int cols;
	int size;
	double* storage[];
	oneDMat(int r, int c);
}

oneDMat::oneDMat(int r, int c, double arr[]) {
	rows = r;
	cols = c;
	size = rows * cols;
	storage[] = arr[];
};



#endif

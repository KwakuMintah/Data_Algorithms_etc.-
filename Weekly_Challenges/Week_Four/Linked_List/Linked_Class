#include <iostream>
using namespace std;
#ifndef NODEHEADERDEF
#define NODEHEADERDEF

//Geeksforgeeks again

struct node {
    double matData;
    struct node* next;
    struct node* prev;
    node(double val) {
        matData = val;
        next = NULL;
        prev = NULL;
    };
};

node;

#endif
